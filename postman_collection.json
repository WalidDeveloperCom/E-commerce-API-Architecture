{
  "info": {
    "name": "E-Commerce API — Django REST Framework",
    "_postman_id": "ecommerce-api-123",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Auth — Register",
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"test@example.com\",\n  \"password\": \"123456\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/auth/register/",
          "host": ["{{base_url}}"],
          "path": ["api", "auth", "register", ""]
        }
      }
    },
    {
      "name": "Auth — Login",
      "request": {
        "method": "POST",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"test@example.com\",\n  \"password\": \"123456\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/auth/login/",
          "host": ["{{base_url}}"],
          "path": ["api", "auth", "login", ""]
        }
      }
    },
    {
      "name": "Products — List",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/api/products/",
          "host": ["{{base_url}}"],
          "path": ["api", "products", ""]
        }
      }
    },
    {
      "name": "Products — Detail",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/api/products/1/",
          "host": ["{{base_url}}"],
          "path": ["api", "products", "1", ""]
        }
      }
    },
    {
      "name": "Cart — Add Item",
      "request": {
        "method": "POST",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"product_id\": 1,\n  \"quantity\": 2\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/cart/add/",
          "host": ["{{base_url}}"],
          "path": ["api", "cart", "add", ""]
        }
      }
    },
    {
      "name": "Order — Create",
      "request": {
        "method": "POST",
        "url": {
          "raw": "{{base_url}}/api/orders/create/",
          "host": ["{{base_url}}"],
          "path": ["api", "orders", "create", ""]
        }
      }
    },
    {
      "name": "Payment — Create Session",
      "request": {
        "method": "POST",
        "url": {
          "raw": "{{base_url}}/api/payments/create-session/",
          "host": ["{{base_url}}"],
          "path": ["api", "payments", "create-session", ""]
        }
      }
    }
  ]
}
